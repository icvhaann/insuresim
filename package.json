{
  "name": "insuresim",
  "version": "5.0.0",
  "description": "Internal sales-training simulator. v5: reinforced validation (anti-repetition + wild-claim), fact sheets, error boundaries, robust downloads.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node tests/scenarios.mjs"
  },
  "engines": { "node": ">=18.0.0" },
  "dependencies": {
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0"
  }
}
